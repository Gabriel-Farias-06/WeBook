spring:
  datasource:
    url: jdbc:postgresql://ballast.proxy.rlwy.net:32664/{PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
